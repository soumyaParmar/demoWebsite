export interface pagesType {
    page:string;
    link:string
};

export interface solutionType {
    solution:string;
    link:string;
};

export interface companyType{
    company:string;
    link:string;
};